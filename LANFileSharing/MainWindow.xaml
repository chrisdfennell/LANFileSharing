<!-- ====================================================================== -->
<!-- == MainWindow.xaml - The User Interface for the WPF Application     == -->
<!-- ====================================================================== -->
<!-- This XAML has been updated to add a throughput column.               -->
<!-- ====================================================================== -->

<Window x:Class="LANFileSharing.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LANFileSharing"
        mc:Ignorable="d"
        Title="LAN File Sharer" Height="700" Width="950" MinWidth="800" MinHeight="600"
        Background="#F0F2F5"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing"
        AllowDrop="True" DragEnter="Window_DragEnter" Drop="Window_Drop">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#A0A0A0"/>
                                <Setter Property="Foreground" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style TargetType="Button" x:Key="SecondaryButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E1E1E1"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="#CCCCCC" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D1D1D1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stop Button Style -->
        <Style TargetType="Button" x:Key="StopButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#D11A2A"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B21623"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.ColumnSpan="2" Grid.Row="0" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="LAN File Sharer" FontSize="24" FontWeight="SemiBold" Foreground="#1F2937"/>
                    <TextBlock x:Name="IpAddressText" Text="Your IP Address: Loading..." FontSize="13" Foreground="#6B7280" Margin="0,4,0,0"/>
                </StackPanel>
                <Button x:Name="SettingsButton" Content="Settings" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="SettingsButton_Click"/>
            </Grid>
        </Border>

        <!-- Left Panel: Discovery -->
        <Border Grid.Column="0" Grid.Row="1" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="Available Devices" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button x:Name="DiscoverButton" Click="DiscoverButton_Click" Style="{StaticResource SecondaryButton}" Padding="8,5" Margin="10,0,0,0" Content="Refresh"/>
                </StackPanel>

                <ListView x:Name="DiscoveredPeersList" Grid.Row="1" SelectionChanged="DiscoveredPeersList_SelectionChanged" BorderThickness="0" Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd" BorderBrush="Transparent" BorderThickness="1" Background="{TemplateBinding Background}" Padding="10" CornerRadius="4" Margin="0,2">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="true">
                                                <Setter TargetName="Bd" Property="Background" Value="#CCE4F7"/>
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0078D7"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>
                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <TextBlock Text="{Binding HostName}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding IpAddress}" Foreground="Gray"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <!-- Right Panel: Send Area -->
        <Border Grid.Column="1" Grid.Row="1" Background="White" Padding="25">
            <StackPanel>
                <TextBlock Text="Send Content" FontSize="20" FontWeight="SemiBold" Foreground="#111827" Margin="0,0,0,10"/>

                <TextBlock Text="1. Select a device from the list on the left" Margin="0,0,0,10" Foreground="#555"/>
                <TextBox x:Name="TargetIpTextBox" IsReadOnly="True" Background="#F0F2F5" Padding="8" BorderThickness="1" BorderBrush="#CCC" Text="No device selected"/>

                <TextBlock Text="2. Choose what to send" Margin="0,20,0,10" Foreground="#555"/>
                <TabControl x:Name="SendTabControl">
                    <TabItem Header="Files / Folders">
                        <StackPanel Margin="0,15,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="BrowseFilesButton" Content="Browse Files..." Style="{StaticResource SecondaryButton}" Click="BrowseFilesButton_Click"/>
                                <Button x:Name="BrowseFolderButton" Content="Browse Folder..." Style="{StaticResource SecondaryButton}" Click="BrowseFolderButton_Click" Margin="5,0,0,0" Grid.Column="1"/>
                                <Border Grid.Column="2" Background="#F0F2F5" Margin="10,0,0,0" CornerRadius="4" BorderBrush="#CCC" BorderThickness="1">
                                    <TextBlock x:Name="FilePathText" Text="No content selected (or drag &amp; drop here)" VerticalAlignment="Center" Margin="10,0" Foreground="#6B7280" TextTrimming="CharacterEllipsis"/>
                                </Border>
                            </Grid>
                            <Button x:Name="SendButton" Content="Send" Style="{StaticResource ModernButton}" Click="SendButton_Click" IsEnabled="False" Margin="0,20,0,0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </TabItem>
                    <TabItem Header="Text / URL">
                        <StackPanel Margin="0,15,0,0">
                            <TextBox x:Name="TextToSendBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="5"/>
                            <Button x:Name="SendTextButton" Content="Send Text" Style="{StaticResource ModernButton}" Click="SendTextButton_Click" Margin="0,10,0,0" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </Border>

        <!-- Bottom Panel: Transfers and Status Log -->
        <GridSplitter Grid.Row="1" Grid.Column="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Background="Transparent"/>
        <TabControl Grid.ColumnSpan="2" Grid.Row="2">
            <TabItem Header="Transfers">
                <ListView x:Name="TransfersList">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="File Name" Width="250" DisplayMemberBinding="{Binding FileName}"/>
                            <GridViewColumn Header="Direction" Width="80" DisplayMemberBinding="{Binding Direction}"/>
                            <GridViewColumn Header="Status" Width="120" DisplayMemberBinding="{Binding Status}"/>
                            <GridViewColumn Header="Progress" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <ProgressBar Value="{Binding Progress}" Maximum="100" Height="18" Width="200"/>
                                            <TextBlock Text="{Binding Progress, StringFormat={}{0:0}%}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" FontSize="12"/>
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Throughput" Width="120" DisplayMemberBinding="{Binding Throughput}"/>
                            <GridViewColumn Header="Actions" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Stop" Style="{StaticResource StopButton}" Click="StopTransferButton_Click" IsEnabled="{Binding IsInProgress}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
            <TabItem Header="Status Log">
                <ListBox x:Name="StatusLog" BorderThickness="0" Background="#FDFDFD" FontFamily="Consolas" FontSize="12"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>

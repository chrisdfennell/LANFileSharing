<!-- Themes/DarkTheme.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- General Colors -->
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="#1e1e1e"/>
    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#252526"/>
    <SolidColorBrush x:Key="TertiaryBackgroundBrush" Color="#3c3c3c"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#444444"/>
    <SolidColorBrush x:Key="TextLogBackgroundBrush" Color="#252526"/>

    <!-- Text Colors -->
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#f1f1f1"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#a0a0a0"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#cccccc"/>
    <SolidColorBrush x:Key="LinkTextBrush" Color="#569cd6"/>
    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#6e6e6e"/>

    <!-- Button Colors -->
    <SolidColorBrush x:Key="PrimaryButtonBackgroundBrush" Color="#007acc"/>
    <SolidColorBrush x:Key="PrimaryButtonHoverBrush" Color="#005a9e"/>
    <SolidColorBrush x:Key="PrimaryButtonForegroundBrush" Color="White"/>

    <SolidColorBrush x:Key="SecondaryButtonBackgroundBrush" Color="#3a3d41"/>
    <SolidColorBrush x:Key="SecondaryButtonHoverBrush" Color="#4a4d51"/>
    <SolidColorBrush x:Key="SecondaryButtonForegroundBrush" Color="#f1f1f1"/>

    <SolidColorBrush x:Key="StopButtonBackgroundBrush" Color="#f44747"/>
    <SolidColorBrush x:Key="StopButtonHoverBrush" Color="#da3b3b"/>

    <SolidColorBrush x:Key="DisabledButtonBackgroundBrush" Color="#555555"/>

    <!-- List/Selection Colors -->
    <SolidColorBrush x:Key="ListSelectedBackgroundBrush" Color="#094771"/>
    <SolidColorBrush x:Key="ListSelectedBorderBrush" Color="#007acc"/>

    <!-- ====================================================================== -->
    <!-- == NEW STYLES FOR DARK THEME IMPROVEMENTS                           == -->
    <!-- ====================================================================== -->

    <!-- TabControl Styling -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1,1,1,0"
                            Margin="0,0,-1,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListView Styling -->
    <Style TargetType="ListView">
        <Setter Property="Background" Value="{StaticResource PrimaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    </Style>

    <Style TargetType="GridViewColumnHeader">
        <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,5"/>
    </Style>

    <!-- ProgressBar Styling -->
    <Style TargetType="ProgressBar">
        <Setter Property="Foreground" Value="{StaticResource PrimaryButtonBackgroundBrush}"/>
        <Setter Property="Background" Value="{StaticResource TertiaryBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"/>
                        <Border Name="PART_Indicator"
                                Background="{TemplateBinding Foreground}"
                                HorizontalAlignment="Left"
                                CornerRadius="2"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>